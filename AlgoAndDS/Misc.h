#pragma once
#include "stdafx.h"
#include <vector>

bool insertInMinHeap(std::vector<int> &, int);
int popFromMinHeap(std::vector<int> &);
int peekHeap(const std::vector<int> &);
void testMinHeap();
void printPascal(int);